//check for ukey
if (!ukey) {
  // return to previous page
  // window.addEventListener('load', function () {
  //   window.history.back();
  // });
  //console.log('go back');
  //window.history.back();
  let previousUrl = window.location;
  console.log(previousUrl);
}
