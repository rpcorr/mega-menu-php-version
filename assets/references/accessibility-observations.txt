Accessibility observations
__________________________

1. When sidebar is open SR announce how to close the sidebar (pressing esc)  in sidebar.js on line 132 I have:

//trapFocus(sidebar);

if this is enabled,

trapFocus(sidebar);

 the user is "trapped" and will cycle through the links in the sidebar

if this is disabled,

//trapFocus(sidebar);

the sidebar close button is within the accessibility tree.  

Which method is preferred?